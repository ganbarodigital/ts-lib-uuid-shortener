{
  "name": "@ganbarodigital/ts-lib-uuid-shortener",
  "version": "0.0.1",
  "description": "Shortens UUIDs in a URL-friendly manner",
  "repository": "github:ganbarodigital/ts-lib-uuid-shortener",
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "npm run clean && npm run lint && tsc",
    "lint": "tslint -p tsconfig.json",
    "prepublishOnly": "npm test && npm run lint",
    "prepack": "npm run build",
    "test": "npm run build && mocha -r ts-node/register \"src/**/*.spec.ts\"",
    "cover": "rimraf coverage && rimraf .nyc_output && nyc --cache=false npm run test",
    "clean": "rimraf lib"
  },
  "contributors": [
    "Stuart Herbert <stuherbert@ganbarodigital.com> (https://ganbarodigital.com)"
  ],
  "keywords": [],
  "files": [
    "lib/**/*",
    "CHANGELOG.md",
    "LICENSE.md",
    "V1/**"
  ],
  "license": "BSD-3-Clause",
  "dependencies": {
    "@ganbarodigital/ts-lib-base-n": "^0.0.2",
    "@ganbarodigital/ts-lib-error-reporting": "^0.2.1",
    "@ganbarodigital/ts-lib-uuid-parser": "^0.3.0"
  },
  "devDependencies": {
    "@types/node": "^13.7.6",
    "tslint": "^5.20.1",
    "typescript": "^3.8.2",
    "@types/chai": "^4.2.9",
    "@types/mocha": "^7.0.1",
    "chai": "^4.2.0",
    "chai-bytes": "^0.1.2",
    "mocha": "^7.0.1",
    "nyc": "^15.0.0",
    "rimraf": "^3.0.2",
    "ts-node": "^8.0.0"
  }
}
